# insertion sort for any number of input
#input:unsorted array
#output: sorted array
array=input().split(',')
j=1

for j in range(j, len(array)):
    key=array[j]
    i=j-1
    while i>=0 and int(array[i])>int(key):
        array[i+1]=array[i]
        i=i-1
        array[i+1]=key
print(array)
